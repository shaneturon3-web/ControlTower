# System State

**Last updated:** 2026-05-17  
**Node:** Set via `~/.machine_env` (not synced)

## Integration health

| Integration | Status |
|-------------|--------|
| `GD` rclone remote | `~/.config/rclone/rclone.conf` — run `rclone config reconnect GD:` then `~/bin/control-tower-resume-apply.sh` |
| Google Drive publish (`GPTs/`, `AIs & System Preferences/`) | Local bundle ready — run `~/bin/estafeta-sync.sh --apply` after rclone install |
| `~/.machine_env` on Latitude | Configured |

## Active phase

`OPERATIONAL_PHASE=Architectural` (ShipYard integration)
